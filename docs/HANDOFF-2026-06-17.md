# Bàn giao / Tiếp tục — 2026-06-17

> Đọc file này để tiếp tục. Mục tiêu đang dở: **bật tài khoản + dự án đám mây** cho bản dùng thật.

## Đã code xong & deploy (code 100% xong, chỉ chờ cấu hình Cloudflare)
Trong đợt này đã làm + push lên `main` (Cloudflare tự build):
- **Trợ lý AI** mở rộng: modify (sửa số đo), giải thích feature, bo/vát/khoét theo vùng, revolve, sweep, loft, text, rib.
- **Nhập STEP/STL** để vẽ tiếp (📥).
- **Tool báo lỗi** 🐞 (lưu D1) + đính kèm ảnh người dùng + trang admin `#feedback-admin`.
- **Tài khoản** (email + mật khẩu, D1) — nút 👤 Đăng nhập + AuthModal.
- **Dự án đám mây** (☁ Dự án) — Tạo/Mở/Lưu/Đổi tên/Xoá; **lưu trong D1** (đã bỏ R2 để không cần thẻ).

## ⚠️ CÒN LẠI: cấu hình Cloudflare (1 lần) — làm trên account **torotic.vn@gmail.com**
> Dùng account cũ torotic.vn (đã có sẵn project Pages + ANTHROPIC_API_KEY + AI Gateway). KHÔNG dùng account kaidant1234 (trống). KHÔNG cần R2 / không cần thẻ.

1. **Tạo D1** database tên `torotic-cad` (Storage & databases → D1 → Create database).
2. Vào D1 → **Console** → dán khối SQL dưới → **Execute**. Gõ `/tables` kiểm tra ra **users, sessions, projects, feedback**.
3. **Workers & Pages → project `torotic-cad` (Pages) → Settings → Bindings → Add → D1 database**: Variable name **`DB`** → chọn `torotic-cad`.
4. **Settings → Variables and Secrets → Add**: **`FEEDBACK_ADMIN_KEY`** = chuỗi bí mật tự đặt.
5. **Deployments → ⋯ → Retry deployment**. Đợi ~1–2 phút.

### SQL để dán (Console):
```sql
create table if not exists users (id integer primary key autoincrement, email text unique not null, password_hash text not null, ten text, created_at text not null default (datetime('now')));
create table if not exists sessions (token text primary key, user_id integer not null references users(id), created_at text not null default (datetime('now')), expires_at text not null);
create index if not exists sessions_user_idx on sessions(user_id);
create table if not exists projects (id integer primary key autoincrement, user_id integer not null references users(id), ten text not null, data text not null default '{"version":1,"features":[]}', size_bytes integer not null default 0, created_at text not null default (datetime('now')), updated_at text not null default (datetime('now')));
create index if not exists projects_user_idx on projects(user_id, updated_at desc);
create table if not exists feedback (id integer primary key autoincrement, loai text not null, mo_ta text not null, modules text, anh text, anh_them text, cay_tinh_nang text, phien_ban text, trang text, trinh_duyet text, man_hinh text, trang_thai text not null default 'moi', ghi_chu_admin text, ly_do_tu_choi text, created_at text not null default (datetime('now')), updated_at text);
create index if not exists feedback_trangthai_idx on feedback(trang_thai, created_at desc);
```

## Test sau khi cấu hình xong
- Chạy localhost: `cd C:\Users\Admin\Desktop\Code3DCad` → `$env:NODE_OPTIONS="--use-system-ca"; npm run dev` → mở http://localhost:5173 (giữ cửa sổ PowerShell mở).
- 👤 Đăng nhập → Đăng ký tài khoản → vẽ vài khối → ☁ Dự án → Lưu → refresh xem còn đăng nhập + còn dự án không.
- 🐞 Báo lỗi → gửi thử → xem ở `…/#feedback-admin` (nhập FEEDBACK_ADMIN_KEY).

## Ghi nhớ môi trường
- Mọi lệnh node/npm cần `NODE_OPTIONS=--use-system-ca`. git dùng schannel.
- Dashboard Cloudflare hay "Internal error" trên máy này → tải lại / dùng Firefox.
- Dự án đám mây giới hạn ~900KB (cây tính năng thường vài chục KB là đủ); dự án nhúng STEP/STL nặng dùng 💾 Lưu file.
- AI chat chạy nhờ ANTHROPIC_API_KEY + CF_AI_GATEWAY đã có sẵn trên account torotic.vn.

## Mốc commit chính đợt này (mới→cũ)
```
98f0e9f Cloud projects: store in D1 instead of R2 (no payment method)
0fa645d Cloud projects (phase 2): create/open/save per account
54509c5 Auth (phase 1): email + password accounts on D1
240eb2c Feedback: user-attached screenshots
b05a99a Feedback / bug-report tool (D1)
2092386 Import STEP/STL
8e183dd rib · 41771c9 text · 1dbe3e4 sweep+loft · 27ae0d0 revolve · 2b3b565 region · 103e0d7 modify
```
